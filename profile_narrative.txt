Table \ref{tab:profile} shows that replacing the manual matmul-softmax stack with PyTorch's fused SDPA kernel improves throughput by **562\%** while reducing peak GPU memory by **70\%**, with similar floating-point work (0.00 TFLOPs vs 0.00 TFLOPs). This kernel-level optimisation compounds with our algorithmic sparsity gains to deliver the end-to-end efficiency improvements reported earlier.